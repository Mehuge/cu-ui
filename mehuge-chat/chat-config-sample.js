ChatConfig = {
    join: [],                // replace with join: [ "channel1", "channel2" ] for as many channels as you want to auto-join
    leave: [],               // replace with leave: [ "channel1", "channel2" ] to not join these channels (e.g. "_combat")
    autoexec: []            // replace with autoexec: [ "command1", "command2" ... ] list of commands to auto run (/ commands or console commands)
};